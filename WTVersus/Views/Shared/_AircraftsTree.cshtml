@model IEnumerable<WTVersus.Models.Plane>;
   
 @foreach (Plane item in Model)
     {
    <div class="@item.Type div-hover" itemscope itemtype = "https://schema.org/aircraft">
        @String.Format("[{0:F1}] ", item.BR)
        <a class="text-dark" asp-controller="Aircrafts" asp-action="Compare"
           asp-route-vehicle1="@item.VehicleId" asp-route-vehicle2="0" itemprop="url">
            @item.Name
        </a>
        <br />
    </div>
    }